import type { FormResetPasswordSubmitionProps } from '../../entities/form-reset-password-submition';

export interface SignInUpProps {
    formComponent: React.FC; 
    imageSrc: string;
    imageAlt:string; 
  }