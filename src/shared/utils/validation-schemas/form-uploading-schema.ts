import * as yup from 'yup';

const schema = yup.object().shape({
    author_name: yup
        .string()
        .required('Поле обязательно для заполнения')
        .trim()
        .min(2, 'Длина поля от 2 до 150 символов')
        .max(150, 'Длина поля от 2 до 150 символов'),
    product_name: yup
        .string()
        .required('Поле обязательно для заполнения')
        .trim()
        .min(1, 'Длина поля от 1 до 150 символов')
        .max(150, 'Длина поля от 1 до 150 символов'),
    category: yup
        .string()
        .required('Поле обязательно для заполнения')
        .trim()
        .min(2, 'Длина поля от 2 до 150 символов')
        .max(150, 'Длина поля от 2 до 150 символов'),
    birth_country: yup
        .string()
        .required('Поле обязательно для заполнения')
        .trim()
        .min(2, 'Длина поля от 2 до 150 символов')
        .max(150, 'Длина поля от 2 до 150 символов'),
    gender: yup
        .string()
        .required('Поле обязательно для заполнения'),
    birth_year: yup
        .string()
        .required('Поле обязательно для заполнения')
        .trim()
        .min(4, 'Длина поля 4 символова')
        .max(4, 'Длина поля 4 символа')
        .matches(
            /^\d{4}$/,
            'Введите корректный год'
        ),
    year: yup
        .string()
        .required('Поле обязательно для заполнения')
        .trim()
        .min(4, 'Длина поля 4 символова')
        .max(4, 'Длина поля 4 символа')
        .matches(
            /^\d{4}$/,
            'Введите корректный год'
        ),
    size: yup
        .string()
        .required('Поле обязательно для заполнения')
        .trim()
        .matches(
            /^\d\d\d\.\d\x\d\d\d\.\dсм$/,
            'Введите корректный рахмер'
        ),
    solo_show: yup
        .string()
        .required('Поле обязательно для заполнения')
        .trim()
        .min(2, 'Длина поля от 2 до 3000 символов')
        .max(3000, 'Длина поля от 2 до 3000 символов'),
    group_show: yup
        .string()
        .required('Поле обязательно для заполнения')
        .trim()
        .min(2, 'Длина поля от 2 до 3000 символов')
        .max(3000, 'Длина поля от 2 до 3000 символов'),
    tablet_material: yup
        .string()
        .required('Поле обязательно для заполнения')
        .trim()
        .min(2, 'Длина поля от 2 до 150 символов')
        .max(150, 'Длина поля от 2 до 150 символов'),
    material: yup
        .string()
        .required('Поле обязательно для заполнения')
        .trim()
        .min(2, 'Длина поля от 2 до 150 символов')
        .max(150, 'Длина поля от 2 до 150 символов'),
});
export default schema;